<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_picking_form_amz_mcf" model="ir.ui.view">
        <field name="name">stock.picking.form.amz.mcf</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <button name="action_assign" position="before">
                <field name="picking_type_code" invisible="1" />
                <field name="amz_mcf_count" invisible="1" />
                <button
                    name="action_amz_mcf_fulfill"
                    type="object"
                    string="Fulfill via Amazon"
                    class="btn-secondary"
                    invisible="picking_type_code != 'outgoing' or amz_mcf_count > 0 or state in ('done', 'cancel', 'draft')"
                />
            </button>
            <div name="button_box" position="inside">
                <button
                    name="action_view_amz_mcf"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-amazon"
                    invisible="amz_mcf_count == 0"
                >
                    <field name="amz_mcf_count" widget="statinfo" string="MCF" />
                </button>
            </div>
        </field>
    </record>
</odoo>
